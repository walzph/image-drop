schemaVersion: v0.2
prepare:
  steps:
    - command: npm install
    - command: npm build
test:
  steps: []
run:
  backend:
    steps:
      - command: npm run server -- --host
    plan: 8
    replicas: 1
    isPublic: false
  frontend:
    steps:
      - command: npm run preview -- --host
    plan: 8
    replicas: 1
    isPublic: true
    network:
      path: /
      stripPath: false
