schemaVersion: v0.2
prepare:
  steps:
    - command: npm install
    - command: npm run build:frontend
test:
  steps: []
run:
  production:
    steps:
      - command: npm run start:production
    plan: 10
    replicas: 1
    isPublic: true
    network:
      path: /
      stripPath: false
